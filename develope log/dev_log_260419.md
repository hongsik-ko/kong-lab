# 공렙 개발 로그 (2026-04-19)

## 1. 종목 상세 API 날짜 기준 확장
- `/api/stocks/{stockId}/issue-summary?date=YYYY-MM-DD` 형태로 확장
- 리스트 API와 동일한 날짜 기준 로직 적용
- 시간 가중치 기준도 동일하게 유지

---

## 2. 환산 가격(KRW) 기능 추가
- ExchangeRate 기반 환율 적용
- `convertedPriceKrw` 필드 추가
- KRW는 그대로, 외화는 환산
- 환율 없을 경우 null 처리

---

## 3. 서비스 계층 책임 분리 강화
- ExchangeRateService 분리
- IssueService는 조합/집계 역할 유지
- 환산 로직 재사용 가능 구조 확보

---

## 4. 이슈 리스트 limit 기능 추가
- `limit` 파라미터 도입
- 상위 N개 결과 반환 가능

---

## 5. offset 기반 페이징 추가
- `offset`, `limit` 조합으로 페이징 구현
- 서비스 레벨에서 페이징 처리 (계산형 구조 유지)
- 응답 구조를 리스트 → 메타 포함 구조로 변경

---

## 6. 페이징 응답 구조 개선
```json
{
  "success": true,
  "data": {
    "items": [...],
    "totalCount": 0,
    "offset": 0,
    "limit": 10,
    "hasNext": true
  }
}
```

---

## 7. 입력값 검증 로직 추가
- offset < 0 → 예외
- limit <= 0 → 예외
- BusinessException + ErrorCode 활용

---

## 8. API 설계 정리
- getIssueList (리스트)
- getIssueSummary (단건 상세)
- 네이밍 일관성 유지 방향 확정

---

## 9. 전체 구조 상태

현재 공렙 구조:

- 이슈 계산 (서비스 기반)
- 대표 뉴스 선정 (규칙 기반)
- 시간 가중치 반영
- 환산 가격 적용
- 페이징/limit 지원
- 공통 응답/예외 처리

👉 단순 CRUD → 분석 서비스 구조 전환 완료

---

## 10. 다음 단계 후보

1. 캐싱 (@Cacheable)
2. 이슈 스냅샷 테이블
3. UI 연결

---

## 한줄 요약

👉 "이슈 분석 API + 페이징 + 환산 가격까지 포함된 MVP 완성"
